# Example .env for AgriConnect cloud backend
# Copy this to .env and fill in real values before running the backend.

# Supabase
SUPABASE_URL=https://your-supabase-url.supabase.co
# For most backend writes you should use a Service Role key with write permissions.
# Keep service keys secret. For local testing you can use an anon key but some
# operations (inserts) may fail if anon key doesn't have adequate permissions.
SUPABASE_KEY=your_supabase_service_or_anon_key

# Optional: a separate service role key if you keep both
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_optional

# MQTT Broker (HiveMQ Cloud or other broker)
# Example: your-cluster.s2.eu.hivemq.cloud
MQTT_BROKER=your_cluster.s2.eu.hivemq.cloud
MQTT_PORT=8883
MQTT_USERNAME=gateway_client
MQTT_PASSWORD=your_mqtt_password

# Farm identifier used by the backend
FARM_ID=FARM-CM-001
